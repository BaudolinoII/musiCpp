#include <iostream>
#include <string>
#include <Windows.h>
#include <cstdio>

//#include "CostumeConsole.h"
//
//AdvCon::AdvanceConsole* AC = AdvCon::AdvanceConsole::getAC();
//
//const size_t scrWidth = 120;
//const size_t scrHeight = 40;

int main() {
	//wchar_t* screen = new wchar_t[scrWidth * scrHeight];
	//HANDLE hc = CreateConsoleScreenBuffer(GENERIC_READ | GENERIC_WRITE, 0, NULL, CONSOLE_TEXTMODE_BUFFER, NULL);
	//SetConsoleActiveScreenBuffer(hc);
	//DWORD dwDW = 0;

	//swprintf_s(screen, 40, L"");
	
	//screen[scrWidth * scrHeight - 1] = '\0';
	//WriteConsoleOutputCharacter(hc, screen, scrWidth * scrHeight, { 0,0 }, &dwDW);
	return 0;
}
